/*#include <Servo.h>
 #include <Arduino.h>
int servoPin = 8;
Servo servo;
int angle = 0;  // servo position in degrees


int degree=180 , time=3;   // degree ,  ms////////////////////////////////////////////////////////////////////////////////////*/

/*void setup() {
    servo.attach(servoPin);
     Serial.begin(9600);

}
void loop() {
 
 int SensorValue=analogRead(A0);

 Serial.println(SensorValue);
    // scan from 0 to 180 degrees
    for(angle = 0; angle < degree; angle++) {
        servo.write(angle);
       delay(time);
    }
     
  //  now scan back from 180 to 0 degrees
    for(angle = degree; angle > 0; angle--) {
        servo.write(angle);
      delay(time);
 }
}*/
